module_name: SE
serial_config:
    - command: set DUAL_GPS_SE_ARGS "-e ${SERIAL_DEV} -g p:${BAUD_PARAM}"
      port_config_param:
        name: GPS_2_CONFIG
        group: GPS
        default: GPS2
      label: GPS 2

